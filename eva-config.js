import { mapping, light as lightTheme } from '@eva-design/eva';
import { DarkTheme } from '@react-navigation/native';

export const eva = {
  mapping,
  themes: {
    light: lightTheme,
    dark: DarkTheme
  },
};
